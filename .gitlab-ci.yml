stages:
  - build

create_file:
  stage: build
  image: alpine:latest
  script:
    - echo "Building ..."
    - mkdir -p build
    - touch build/somefile.txt
  artifacts:
    paths:
      - build/somefile.txt